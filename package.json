{
  "name": "radmail-mcp",
  "version": "0.1.0",
  "private": false,
  "description": "RadMail MCP server — email operating system for agents: two-axis triage (importance x urgency), a 'Right Now' lane, commitment follow-through, reviewable drafts, and a machine-verifiable BEC hard-stop (money / changed-banking / first-contact / decision / injection = human-only forever). Zero-auth in-memory sandbox.",
  "license": "MIT",
  "author": "RadMail (https://radmail.ai)",
  "type": "module",
  "main": "dist/src/index.js",
  "bin": {
    "radmail-mcp": "dist/src/index.js"
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx test/firewall.test.ts test/taint.test.ts",
    "start": "node dist/src/index.js"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "email",
    "triage",
    "inbox",
    "agents",
    "bec",
    "security",
    "productivity"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  }
}
